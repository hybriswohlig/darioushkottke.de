# Admin Panel Additional Security Layer
# Uncomment the lines below to add HTTP Basic Authentication as an extra layer

# AuthType Basic
# AuthName "N&E Innovations Admin Area"
# AuthUserFile /full/path/to/.htpasswd
# Require valid-user

# Note: To enable this:
# 1. Uncomment the lines above
# 2. Change /full/path/to/ to your actual server path
# 3. Create .htpasswd file using: htpasswd -c .htpasswd admin
# 4. This adds a second layer of protection before PHP login

# Prevent directory listing
Options -Indexes

# Protect sensitive files
<FilesMatch "(\.htaccess|\.htpasswd)">
    Order allow,deny
    Deny from all
</FilesMatch>
